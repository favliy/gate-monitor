name: Keep Render Alive
on:
  schedule:
    - cron: '*/4 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://gate-monitor-yhab.onrender.com
